const express = require('express')// importamos 
const app = express() // la app va a ser express
app.get("/",(req,res)function{
	res.send("hellooo")
})
app.listen(3000); //escucha a loclahostconst express = require('express')// importamos 
const app = express() // la app va a ser express
app.get("/",(req,res)function{
	res.send("hellooo")
})
app.listen(3000); //escucha a loclahostconst express = require('express')// importamos 
const app = express() // la app va a ser express
app.get("/",(req,res)function{
	res.send("hellooo")
})
app.listen(3000); //escucha a loclahostconst express = require('express')// importamos 
const app = express() // la app va a ser express
app.get("/",(req,res)function{
	res.send("hellooo")
})
app.listen(3000); //escucha a loclahostconst express = require('express')// importamos 
const app = express() // la app va a ser express
app.get("/",(req,res)function{
	res.send("hellooo")
})
app.listen(3000); //escucha a loclahostconst express = require('express')// importamos 
const app = exp